---
layout: base.njk
eleventyImport:
  collections: ["eleventyJs"]
---
{% include 'sitenav.njk' %}
<header class="tdbc-hero tdbc-background--texture tdbc-background--primary">
  <div class="tdbc-content-maxlength">
    <h1>{{ title }}</h1>
    <p class="tdbc-lead">Filters, shortcodes, and other tips for extending 11ty</p>
  </div>
</header>
<main id="main" tabindex="-1" class="tdbc-container tdbc-mx-auto">
  <div class="tdbc-column-container">
      {%- for post in collections.eleventyjs | reverse %}
      <article id="{{ post.data.title | slug }}" class="tdbc-card tdbc-background--texture">
        <div class="tdbc-card__content">
          <h2>{{ post.data.title }}</h2>
          <p>{{ post.data.description }}</p>
          <a href="{{ post.url }}" class="tdbc-button tdbc-button--text" aria-label="View samples of {{ post.data.title }}">View Samples</a>
        </div>
      </article>
      {% endfor -%}
  </div>
</main>
{% include 'webring.njk' %}